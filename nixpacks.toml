# Railway: FFmpeg in the image (Ken Burns / final render). Root package.json "start" → Next.js web.
#
# Railpack requires a root "start" script (see package.json).
# Web service: use default start (npm start).
# Worker service: Railway → Settings → Deploy → Start Command: npm run start:worker

[phases.setup]
aptPkgs = ['ffmpeg']
